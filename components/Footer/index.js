import React from 'react';
import PropTypes from 'prop-types';

function Footer({}) {
  return (
    <footer>
      <p>NextJS Boilerplate</p>
    </footer>
  );
}

Footer.defaultProps = {};

Footer.propTypes = {};

export default Footer;
